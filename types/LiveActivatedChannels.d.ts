export type LiveActivatedChannelsChannel = {
  id: string,
  username: string,
  game: string,
  title: string,
  view_count: string
};
export type LiveActivatedChannels = {
  channels: LiveActivatedChannelsChannel[];
};
